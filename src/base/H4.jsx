import styled from "@emotion/styled";

const DialogTitle = styled.h4`
  font-weight: 600;
  font-size: 20px;
  line-height: 24px;
  color: ${({ color }) => color};
  margin: 0px;
`;

export default DialogTitle;
